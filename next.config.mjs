/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "st.depositphotos.com",
      "thumbs.dreamstime.com"
    ],
  },
  /* config options here */
  reactCompiler: true,
};

export default nextConfig;
